# web/

Stage 4 deliverable (not yet built). Will hold a single plain HTML/JS page
that submits feature values to the Stage 3 API and displays the predicted
failure risk plus its explanation. Function over polish — no build step,
no framework unless it turns out to be genuinely easier than vanilla JS.
