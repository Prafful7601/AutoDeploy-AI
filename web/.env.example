# Copy to .env.local (gitignored by Vite's default .gitignore) and adjust
# if the backend isn't running on the default port. Not a secret — this
# is just where the browser sends requests; the GitHub token lives
# server-side only, in the backend's own .env (see ../.env.example).
VITE_API_BASE_URL=http://localhost:8000
